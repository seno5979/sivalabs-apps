<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springdemo.dao.UserAccountDAO">

	<resultMap id="UserAccountResultMap" type="UserAccount">
		<id property="id" column="ID" />
		<result property="firstName" column="FIRST_NAME" />
		<result property="lastName" column="LAST_NAME" />
		<result property="birthDate" column="BIRTH_DATE" />
	</resultMap>
	
	<select id="getUserAccountById" resultMap="UserAccountResultMap" parameterType="java.lang.Integer">
		SELECT ID, FIRST_NAME, LAST_NAME, BIRTH_DATE
		FROM USER_ACCOUNT
		WHERE ID = #{id,jdbcType=INTEGER} 
	</select>
	
	<select id="getAllUserAccounts" resultMap="UserAccountResultMap">
		SELECT ID, FIRST_NAME, LAST_NAME, BIRTH_DATE
		FROM USER_ACCOUNT
	</select>

	<insert id="create" parameterType="UserAccount">
	    insert into user_account (FIRST_NAME, LAST_NAME, BIRTH_DATE)
    	values (#{firstName,jdbcType=VARCHAR}, 
    			#{lastName,jdbcType=VARCHAR}, 
   				#{birthDate,jdbcType=DATE}
      			)
	    <selectKey resultType="java.lang.Integer" keyProperty="id" >
	      SELECT LAST_INSERT_ID()
	    </selectKey>
	</insert>
	
	<update id="update" parameterType="UserAccount" >
	    update user_account
	    set FIRST_NAME = #{firstName,jdbcType=VARCHAR},
	      LAST_NAME = #{lastName,jdbcType=VARCHAR},
	      BIRTH_DATE = #{birthDate,jdbcType=DATE}
	    where ID = #{id,jdbcType=INTEGER}
	</update>
	
	<delete id="delete" parameterType="UserAccount" >
	    delete from user_account
	    where ID = #{id,jdbcType=INTEGER}
  	</delete>
  	
</mapper>